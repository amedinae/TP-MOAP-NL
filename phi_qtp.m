function x_next = phi_qtp(x, u, options)
% X_NEXT=PHI_QTP(X, U) compute next state from current state X and control input U.
%
% Simulation function in order to compute the next value in the quad tank
% process. You can optionnally pass a sampling period with the key 'dt' (default
% value is 1).
end
